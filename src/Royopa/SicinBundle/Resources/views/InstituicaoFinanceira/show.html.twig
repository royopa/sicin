{% extends '::base.html.twig' %}

{% block body -%}
    <h1>InstituicaoFinanceira</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Cnpj</th>
                <td>{{ entity.cnpj }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ entity.nome }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('instituicao_financeira') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('instituicao_financeira_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
